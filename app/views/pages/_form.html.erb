<div class="row">
  <div class="span6 offset3">
    <%= form_for(@page) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

        <%= f.label :title %><br>
        <%= f.text_field :title %>

        <%= f.label :body %><br>
        <%= f.text_area :body %>

        <%= f.submit "Criar página", class: "btn btn-large btn-primary" %>
      <% end %>
  </div>
</div>

<tr>
  <td>
    <%= link_to user.name, user %>
  </td>
  <td>
    <% if current_user.admin? && !current_user?(user) %>
      <%= link_to t('.delete'), user, 
        class: "btn btn-danger",
        method: :delete, confirm: t('.confirmation')  %>
    <% end %>
  </td>
</tr>

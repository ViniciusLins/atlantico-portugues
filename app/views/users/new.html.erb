<% provide(:title, 'Criar usuário') %>
<h1>Criar usuário</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Criar usuário", class: "btn btn-lg btn-primary" %>
    <% end %>
  </div>
</div>
